spring:
  datasource:
    url: jdbc:mysql://localhost:3306/credit_card_app
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  main:
    allow-circular-references: true

logging:
  file:
    path: .